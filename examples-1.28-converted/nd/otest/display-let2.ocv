const c0, c1: bitstring.

process
	Ostart() :=
r: bitstring <- (let x: bitstring = c0 in x else c1);
return(r)



(* EXPECTED
All queries proved.
0.024s (user 0.024s + system 0.000s), max rss 10384K
END *)
