param N.

type key [fixed, large].

query secret k [cv_onesession].
query secret k.

process
        Oa() :=
k <-R key;
return();
foreach i_1 <= N do
Ob(s: key) :=
if k = s then
  return(s)
else
  return(s)



(* EXPECTED
All queries proved.
0.016s (user 0.016s + system 0.000s), max rss 10284K
END *)
