type G [large,nonuniform,bounded].
type subG [large,nonuniform,bounded].
type Z [large,nonuniform,bounded].

expand DH_X25519(G, Z, g, exp, mult, subG, g_k, exp_div_k, exp_div_k', pow_k, subGtoG, zero, sub_zero).

channel c.

event bad.

query event(bad) ==> false.

process
	in(c, X: subG);
	y <-R Z;
	if pow_k(subGtoG(exp_div_k(g_k, y))) = X then
	event bad

(* EXPECTED
All queries proved.
0.164s (user 0.148s + system 0.016s), max rss 44236K
END *)
