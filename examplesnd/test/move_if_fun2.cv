channel c.

proof {
    move_if_fun to_term
 }

process
   in(c, (b:bool, b':bool, x:bitstring, y:bitstring));
   out(c, if_fun(b,x,if_fun(b', y, x)))

(* EXPECTED
All queries proved.
0.043s (user 0.023s + system 0.020s), max rss 18168K
END *)
