(* The constant cst_bool was also generated by CryptoVerif with type bool *)

type key [large,fixed].
type cleartext.
type ciphertext.

proba Penc.

expand IND_CPA_sym_enc(key, cleartext, ciphertext, enc, dec, injbot, Z, Penc).

channel c.

process
	(in(c, ct: ciphertext); new cst_bool: key; if defined(b) then out(c, dec(ct, cst_bool))) | (in(c, ct: cleartext); let b: bool = true in out(c, ct))

(* EXPECTED
All queries proved.
0.164s (user 0.136s + system 0.028s), max rss 44044K
END *)
